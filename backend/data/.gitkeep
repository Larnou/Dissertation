# Плейсхолдер, чтобы директория `data` попала в git.

# Здесь будут храниться:
# - сырые данные, загруженные с CDAWEB и других источников (`raw/`);
# - промежуточные датасеты (`interim/`);
# - обработанные и готовые к анализу выборки (`processed/`).

